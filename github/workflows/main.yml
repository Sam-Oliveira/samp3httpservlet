name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.c60212cb-aa16-4b80-962d-270b89c55d92}}
          heroku_app_name: "samp3httpservlet" #Must be unique in Heroku
          heroku_email: "sam.oliveira.1331@gmail.com"